package ${groupId}.${lowerCaseTableName}.dto;

import ${groupId}.${lowerCaseTableName}.entity.${entityClassName}Entity;
import lombok.experimental.SuperBuilder;

@SuperBuilder
public class ${entityClassName}DTO extends ${entityClassName}Entity {
    
}